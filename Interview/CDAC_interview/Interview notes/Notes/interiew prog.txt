/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby, 
C#, VB, Perl, Swift, Prolog, Javascript, Pascal, HTML, CSS, JS
Code, Compile, Run and Debug online from anywhere in world.

Editor-Nitin Satle
*******************************************************************************/
/***************middle of an array**************/
/*#include <stdio.h>
int main()
{
    int n;
    printf("number of array ele");
    scanf("%d",&n);
    int arr[]={10,20,30,40,50};
    int count=0;
    int i;
    for(i=0;i<n;i++){
        count++;
}
int midd = count /2;
printf("%d",arr[midd]);
 return 0;
}*/





/****************Revrse string using ptr*****************/
/*#include<stdio.h>
#include<string.h>
int main(){
    char *str = "Nitin";
    int a = strlen(str);
    for(int i=a;i>=0;i--){
        printf("%c",*(str+i));
    }
    // printf("%s",str);
   // int a= strlen(*ptr);
   // printf("%d",a);
    return 0;
}
*/
    
    

/******************power of 2 program****************/
#include<stdio.h>
#include<string.h>

int main(){
    int n;
    printf("enetr a number the power want");
    scanf("%d",&n);
    
    int i;
    for(i=1;i<11;i++){
        int a = 2<<i;
        printf("%d\n",a);
    }
    return 0;
}
        
/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

Editor-Nitin Satle
*******************************************************************************/
/*************************power of 2**********************/
/*#include <stdio.h>

int main()
{
   // printf("Hello World");
   int n;
   printf("enetr the number which u want to power");
   scanf("%d",&n);
   
   int pow;
   printf("enetr the range of power");
   scanf("%d",&pow);
   
   int c;
   
   
   int i;
   for(i=0;i<=pow;i++){
    c = n<<i;
    printf("%d ",c);
   }

    return 0;
}
*/

/*******************************palidrome program string**********************/
/*#include<stdio.h>
#include<string.h>
int main(){
    char m1[]="dad";
    int a =strlen(m1);
    int flag=0;
   // printf("%d",a);
    
    int i,j;
    for(i=0;i<=a;++i){
        if(m1[i] == m1[a-i-1])
            flag = flag+1;
     }
    //printf("%d",flag);
    if(flag){
        printf("palidroem");
    }else{
        printf("not");
    }
    return 0;
}
*/

/************palindrome number*****************/
/*#include<stdio.h>

int main(){
    int num;
    printf("enter num");
    scanf("%d",&num);
    
    int n=5;   //nor of digit
    
    int flag=0;
    int i;
    int rem;
    
    for(i=0;i<n;i++){
        rem = num%10;
        num = num/10;
        if(rem == num){
            ++flag;
        }
    }
    
    if(flag){
        printf("pali");
    }else{
        printf("not");
    }
    return 0;
}
    
    
    
  */
 

/*********************fibanocci series************/
/*#include<stdio.h>
int main()
{
    int n1=0,n2=1,n3;
    printf("%d %d",n1,n2);
    int i=2;
    
    while(i<10){
        n3 = n1+n2;
        printf(" %d",n3);
        n1=n2;
        n2=n3;
        i++;
    }
    return 0;
}
  */  
  /*#include<stdio.h>
  
  int main(){
      int num,rem,rev;
      printf("Enter a num");
      scanf("%d",&num);
      
      int original=num;
      int i=0;
      while(i<5){
          rem= num%10;
          rev = rem+i*10;
          num=num/10;
          i++;
        }
        printf("%d",rev);
        if(original == rev){
            printf("pali");
        }else{
            printf("not");
            
        
        }
        return 0;
  }
*/

/*************fabonocci series using pointer ********/

/*
#include<stdio.h>

int main()
{
	int i, term;
	int *ptr;
	int first=0,second=1,next;
	ptr=&term;

	printf("How many no you want to printf\n");
	scanf("%d",&term);
	
	printf("Fabonacci series\n");

	printf("%d,%d ",first,second);

	for(i=2;i<*ptr;i++)
	{
		next=first+second;
		printf("%d ",next);

		first=second;
		second=next;
	}
	return 0;
}
*/


/***************copy string from one to other*******************/
/*
#include<stdio.h>
#include<string.h>
int main(){
    char s1[] ="Nitin";
    int a = strlen(s1);
    char s2[a];
    
    for(int i=0;s1[i] !='\0';i++){
        s2[i] = s1[i];
    }
    printf("%s",s2);
    return 0;

    }
*/

/*************count repeted no*****************/
/*#include<stdio.h>
int main(){
    int arr[]={1,2,3,2,1};
    int len = sizeof(arr)/sizeof(int);
    int count=0;
    int i,j=0;
   // int count =0;
    int rep;
    
    printf("enetr repet num count");
    scanf("%d",&rep);
    
    for(i=0;arr[i]==rep;++i){
       for(j=i+1;arr[j]==rep;++j){
        if(arr[i] == arr[j])
          printf("%d ",arr[i]);
        
    }
    }

    return 0;
}

*/

/*******************sort array in descending order*****************/
/*#include<stdio.h>
int main(){
    int arr[]={1,2,3,4,5};
    int len = sizeof(arr)/sizeof(int);
    int i=0;
   
    for(i=len-1;i>=0;i--){
        printf("%d ",arr[i]);
     }
    return 0;
}*/


/******************************set bit******************/
/*#include<stdio.h>
int main(){
    int n = 15;
    int a = ~(1<<2)&n;
    printf("%d",a);
    return 0;
}
  */

/*  
 #include<stdio.h>
 int main(){
     char a[]="Nitin";
     int i,count=0;
     for(i=0;a[i]!='\0';i++){
         count++;
         
     }
     printf("length=%d\n",count);
    for(i=count-1;a[i]>=0;i--){
        printf("%c",a[i]);
    }     
    return 0;
 }

  */
    
/********** C program to set the nth bit of a number *******************/
/*

#include <stdio.h>

int main()
{
    int num, n, newNum;

    /* Input number from user */
    printf("Enter any number: ");
    scanf("%d", &num);

    /* Input bit position you want to set */
    printf("Enter nth bit to set (0-31): ");
    scanf("%d", &n);

    /* Left shift 1, n times and perform bitwise OR with num */
    newNum = (1 << n) | num;

    printf("Bit set successfully.\n\n");
    printf("Number before setting %d bit: %d (in decimal)\n", n, num);
    printf("Number after setting %d bit: %d (in decimal)\n", n, newNum);

    return 0;
}

*/


*/

//what is null charactor-it has considered as the terminating character used in strings to notify the end of the string to the complier
/*
#include<stdio.h>
#include<string.h>

int main(){
	int length;
	char ch[20]={'n','i','t','i','n','\0'};
	//char ch[20]={"Nitin Satle"};
	printf("lenth of the string is : %ld",strlen(ch));
	return 0;

}

*/

//swap the variable
/*
#include<stdio.h>
#include<stdlib.h>

int main(){
	int a=10;
	int b=20;
	
	printf("before swap a = %d b = %d\n",a,b);
	
	int temp;
	temp = a;
	a = b;
	b = temp;
	
	printf("after swaping a = %d b = %d\n",a,b);
	
	return 0;

}


*/

//swap two number using pointer
/*
#include <stdio.h> 
int main()
{
	int var1, var2, *num1, *num2, temp;
	printf("Enter the value of var1 and var2n\n");
	scanf("%d%d", &var1, &var2);
	printf("Before Swappingnvar1 = %dnvar2 = %d\n", var1, var2);
	num1 = &var1;
	num2 = &var2;
	temp = *num2;
	*num2   = *num1;
	*num1   = temp;
	printf("After Swappingnvar1 = %dnvar2 = %d\n", var1, var2);
	return 0;
}
*/
/*
//swap two number without using third variable
#include<stdio.h>

int main(){
	int a=10,b=20;
	
	printf("Before swap a = %d b = %d\n",a,b);
	
	//using Addition and substraction
	a = a + b;	//10+20=30	a=30
	b = a - b;	//30-20=10	b=10
	a = a - b;	//30-10=20	a=20
	
	//using multiplication and diision
	//a = a * b;	//a=200 (10*20)    
	//b = a / b;	//b=10 (200/20) 
	//a = a / b;	//a=20 (200/10)    
	
	printf("After swaping function a = %d b = %d",a,b);
	
	return 0;
	
}
*/

//how to determine endianness
/*
#include<stdio.h>
#define BIG_ENDIAN 0
#define LITTLE_ENDIAN 1
int main(void)
{
short int word = 0x0001;
char *byte = (char *) &word;
return(byte[0] ? LITTLE_ENDIAN : BIG_ENDIAN);
}
*/
//This code assigns the value 0001h to a 16-bit integer. A char pointer is then assigned to point at the
//first (least-signifi cant) byte of the integer value. If the fi rst byte of the integer is 01h, then the system
//is little-endian (the 01h is in the lowest or least-signifi cant address). If it is 00h then the system is
//big-endian.




//print the pattern

/*
#include<stdio.h>

int main(){
	int i,j,row;
	
	printf("Enter the number of rows\n");
	scanf("%d",&row);
	
	for(i=1;i<=row;i++){
		for(j=1;j<=i;j++){
			printf("%d",j);
		}
		printf("\n");
	}
	return 0;
}
*/

//Explain #pragma derective

//1. This is a preprocessor directive used to turn on or off certain feture
//2. It is of two types of #pragma startup,#pragma exit and pragma warn.
//3. #pragma startup allows us to specify  functions called upon programm startup.
//4. #pragma exit allows us to specify functions called upon programm exits.
//5. #pragma warn tells the computer to suppress any warning or not.



#include<stdio.h>    
#include<stdlib.h>  
int main(){  
int a[10],n,i;    
system ("cls");  
printf("Enter the number to convert: ");    
scanf("%d",&n);    
for(i=0;n>0;i++)    
{    
a[i]=n%2;    
n=n/2;    
}    
printf("\nBinary of Given Number is=");    
for(i=i-1;i>=0;i--)    
{    
printf("%d",a[i]);    
}    
return 0;  
}  

















/****************************** OS ********************/
//#include<stdio.h>
/*
main()
{
		printf("hello world\n");
	fork();
	fork();
		printf("hello world\n");
	fork();
	fork();
}
*/
/*
main() {
        int i;
        for (i = 0; i < 5; i++)
        fork();
   
        printf("\nmy pid = %d\n", getpid());
    }

*/

/*
int main() {
        fork();
        fork();
        fork();
        if (wait(0) == -1)
            printf("leaf child\n");
    }
*/

/*****************************************************/




    

